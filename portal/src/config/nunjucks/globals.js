// Nunjucks globals — values injected into the Nunjucks environment.
//
// Currently empty. GOV.UK Frontend 6.x uses the refreshed branding by
// default so govukRebrand is no longer needed.

export {}
